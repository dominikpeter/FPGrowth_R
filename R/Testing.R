#
# # rm(list = ls())
#
# # library(FPGrowth)
#
# # Sys.setenv("PKG_CXXFLAGS"="-std=c++11")
#
#
# a <- "A"
# b <- "B"
# c <- "C"
# d <- "D"
# e <- "E"
#
# lst <- list(
#               c( a, b ),
#               c( b, c, d ),
#               c( a, c, d, e ),
#               c( a, d, e ),
#               c( a, b, c ),
#               c( a, b, c, d ),
#               c( a ),
#               c( a, b, c ),
#               c( a, b, d ),
#               c( b, c, e )
#               )
#
# test(lst)


